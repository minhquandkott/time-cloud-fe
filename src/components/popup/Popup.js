import React from "react";
import PropTypes from "prop-types";

const Popup = (props) => {
  return (
    <div className="popup">
      <p className="title"></p>
    </div>
  );
};

Popup.propTypes = {};

export default Popup;
